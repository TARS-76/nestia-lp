Wide-tracked uppercase Latin micro-label that sits above section headings.

```jsx
<Eyebrow dot>Menu</Eyebrow>
<Eyebrow tone="plum">Access</Eyebrow>
```

`dot` adds a petal-rouge dot. `tone`: `muted` (default) · `plum` · `cream` (on dark).
