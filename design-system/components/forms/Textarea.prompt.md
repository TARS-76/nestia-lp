Multiline text area with the same field styling as Input — for requests, notes, message bodies.

```jsx
<Textarea label="ご要望" rows={5} placeholder="気になる箇所やご希望をお聞かせください" />
```

Same `label` / `hint` / `error` props as Input. Resizes vertically.
